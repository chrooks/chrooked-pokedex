__pycache__/
*.pyc
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.venv/
venv/
.DS_Store
.playwright-mcp/

# Root-level vitest cache (frontend tests run from the repo root drop it here)
/node_modules/

# Scratch data dumps
/scratch_dex.json

# Frontend build artifacts
frontend/node_modules/
frontend/dist/
frontend/*.tsbuildinfo
frontend/vite.config.js
frontend/vite.config.d.ts

# Target registry — machine-specific game paths, not canon
targets.json
.chrooked/targets.json

# Plans/throughlines are local-only by default, except the Essentials
# behavior-porting track docs, which travel with that work.
feature_requests/*

# Dispatched-agent git worktrees (transient, isolated checkouts)
.claude/worktrees/

# Local secrets/config (keep .env.example committed)
.env
!.env.example

# Cowork handoffs/runtime — machine-local
.cowork/
chrooked.log
.diagram-exports/
